<?xml version="1.0" encoding="utf-8"?>
<!--
  ADD these permissions to your existing AndroidManifest.xml
  File location: android/app/src/main/AndroidManifest.xml

  Only add the lines that are NOT already present.
  The <application> block changes are the most important.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.paperpress.app">

    <!-- ── REQUIRED PERMISSIONS ─────────────────────────────────────── -->

    <!-- Internet: for Supabase auth (still needed even in offline PDF mode) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Storage: to save PDFs to Documents/PaperPress/ -->
    <!-- On Android 10+, this is not needed (scoped storage handles it) -->
    <!-- On Android 9 and below, this IS needed -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false">

        <!-- ── IMPORTANT: Add this FileProvider for PDF file sharing ── -->
        <!-- Without this, FileOpener plugin cannot open the saved PDF  -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
